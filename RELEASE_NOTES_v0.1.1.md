# Data Exchange Tools v0.1.1

เวอร์ชันนี้ปรับปรุงความแม่นยำของการจับคู่ข้อมูลบุคคล และการตรวจสอบข้อมูลการตายกับส่วนกลาง

## สิ่งที่ปรับปรุง

- เปลี่ยนลำดับการจับคู่ให้ตรวจ `HOSCODE + PID` กับ `opdconfig.hospitalcode + person.person_id` ก่อน
- ถ้าจับคู่ PID ไม่ได้ ระบบจึงตรวจด้วย `CID 9 หลัก + SEX + BIRTH + NAME + LNAME`
- ลบเครื่องหมาย `*` จาก `NAME` และ `LNAME` ก่อนเทียบชื่อแบบขึ้นต้นกับ `person.fname` และ `person.lname`
- รองรับกรณีพบ person หลายรายการที่มี CID 9 หลัก เพศ และวันเกิดตรงกัน โดยใช้ชื่อและนามสกุลช่วยคัดรายการที่ถูกต้อง
- เพิ่มตัวกรองผลลัพธ์:
  - จับคู่ PID ได้
  - จับคู่ PID ไม่ได้
  - จับคู่ CID ได้
  - จับคู่ CID ไม่ได้
- เพิ่มคอลัมน์ `เงื่อนไขที่ใช้` และ `เทียบตาย` ในตารางและไฟล์ Excel ที่ส่งออก
- ส่งเฉพาะ `PERSON_CID` ที่สมบูรณ์จากรายการที่จับคู่สำเร็จไปตรวจฐานคนตายกลาง
- เมื่อ API Center ติดต่อได้แต่ไม่พบข้อมูล ระบบแสดงผลเป็น `0` แทนสถานะใช้งานไม่ได้
- หาก API Center หรืออินเทอร์เน็ตขัดข้อง การแปลงข้อมูล HIS และหน้าประวัติยังใช้งานได้ตามปกติ
- ปรับการแสดงผลการ์ดตัวกรองให้รองรับหน้าจอหลายขนาด

## การอัปเดต

ผู้ดูแลระบบสามารถเปิดหน้า **ตั้งค่า** แล้วกด **เช็ก Update** จากนั้นติดตั้งเวอร์ชัน `v0.1.1` ได้โดยตรง

หลังอัปเดตสำเร็จ ระบบจะรีสตาร์ต Agent และใช้กติกาการจับคู่ใหม่กับการแปลงข้อมูลครั้งถัดไป ประวัติที่สร้างด้วยเวอร์ชันเดิมจะไม่ถูกประมวลผลย้อนหลัง

## ไฟล์สำหรับ GitHub Release

- `DataExchangeTools.exe`
- `latest.json`
- `DataExchangeTools-v0.1.1.exe` (สำหรับดาวน์โหลดติดตั้งด้วยตนเอง)
