# Data Exchange Tools v0.1.2

เวอร์ชันนี้เพิ่มระบบตรวจสอบคุณภาพข้อมูลแบบกำหนดรายงานจาก Control ปรับปรุงการตรวจสอบสถานะการเสียชีวิต และขยายความเข้ากันได้ของฐานข้อมูล MariaDB ตั้งแต่รุ่น 5.x ถึงรุ่นปัจจุบัน

## สิ่งที่เพิ่มและปรับปรุง

### ตรวจสอบคุณภาพข้อมูล

- เพิ่มเมนู `ตรวจสอบคุณภาพข้อมูล` ซึ่งรับรายชื่อรายงาน เงื่อนไข SQL แบบ `SELECT` คอลัมน์ และตัวกรองจาก Control
- เพิ่มรายงานแรก `ตรวจสอบน้ำหนัก/ส่วนสูงผิดปกติ` โดยเลือกข้อมูลคัดกรองล่าสุดของแต่ละคนที่ยังมีชีวิตอยู่ใน PERSON
- เพิ่มการ์ดสรุป `ทั้งหมด`, `ปกติ` และ `ผิดปกติ` พร้อมตัวกรองน้ำหนักผิดปกติ ส่วนสูงผิดปกติ และผิดปกติทั้งสองค่า
- รองรับช่วงวันที่ตามปีงบประมาณปัจจุบัน หรือช่วงวันที่ที่ Control กำหนด และ Control สามารถเลือกซ่อนตัวกรองวันที่สำหรับรายงานที่ไม่ต้องใช้ช่วงเวลาได้
- เพิ่มคำอธิบายหลักการตรวจจับข้อมูลผิดปกติแบบพับ/ขยาย
- เพิ่มการค้นหา การเรียงทุกคอลัมน์ การแบ่งหน้า และการส่งออก Excel ทั้งหมด/ตามตัวกรอง
- ใช้ข้อมูล cache ใน Agent เมื่อกดการ์ด ค้นหา เรียง หรือเปลี่ยนหน้า และดึงข้อมูลจาก HIS ใหม่เฉพาะเมื่อเข้ารายงานใหม่หรือกด `โหลดข้อมูลใหม่`
- จำกัดคำสั่งรายงานที่ Agent ยอมรับให้เป็น `SELECT` เดียว และเปิด transaction แบบอ่านอย่างเดียวเมื่อฐานข้อมูลรองรับ

### ตรวจสอบสถานะการเสียชีวิต

- แยกคอลัมน์อายุไว้ถัดจากวันเกิด และคำนวณอายุเป็นปีจากวันเกิดเทียบวันปัจจุบัน
- เพิ่มการเรียงข้อมูลทุกคอลัมน์
- ปรับขนาดช่องค้นหา ปุ่มส่งออก ตาราง และรูปแบบหัวคอลัมน์ให้สอดคล้องกับหน้าแปลงข้อมูล
- เพิ่ม Loading เต็มหน้าระหว่างตรวจสอบ และใช้ผลลัพธ์ในหน่วยความจำสำหรับการกรอง/เรียงโดยไม่เรียก API ซ้ำ

### การเชื่อมต่อฐานข้อมูล

- รองรับ MariaDB 5.x และ MariaDB/MySQL รุ่นใหม่ด้วยการตรวจรูปแบบ handshake และเลขรุ่นฐานข้อมูล
- ทดลองเชื่อมต่อด้วย `utf8mb4` ก่อน และ fallback เป็น `utf8` อัตโนมัติเมื่อฐานรุ่นเก่าไม่รองรับ โดยยังรองรับข้อความภาษาไทย
- รองรับ transaction fallback สำหรับ MariaDB รุ่นเก่าที่ไม่รู้จัก `START TRANSACTION READ ONLY`
- เปลี่ยน pagination จาก `SQL_CALC_FOUND_ROWS` เป็น `COUNT(*)` เพื่อรองรับฐานข้อมูลรุ่นใหม่ต่อเนื่อง
- ปรับข้อความแจ้งข้อผิดพลาดการเชื่อมต่อให้ระบุสาเหตุ เช่น สิทธิ์ไม่พอ host ถูกปฏิเสธ authentication plugin และ SSL/TLS
- อัปเดต PyMySQL เป็น 1.2.0

### หน้าประวัติและการแปลงข้อมูล

- ปรับตำแหน่งหัวคอลัมน์และปุ่มดำเนินการในหน้าประวัติให้เป็นระเบียบ
- รองรับการกลับไปดำเนินการต่อจากรายการที่อัปโหลดแล้ว และลบประวัติพร้อมไฟล์ที่เกี่ยวข้อง
- เพิ่มการส่งออก Excel ทั้งหมด/ตามตัวกรอง และไฟล์แก้ไข PERSON ตามเงื่อนไขที่เลือก

## ข้อควรทราบก่อนอัปเดต

- ระบบไม่เขียนแก้ไขข้อมูลกลับ HIS; การตรวจสอบคุณภาพข้อมูลทำงานด้วย `SELECT` เท่านั้น
- รายงานตรวจสอบคุณภาพข้อมูลต้องใช้งานร่วมกับ API Center ที่มีนิยามรายงานและสิทธิ์ Agent ที่ถูกต้อง
- บัญชีฐานข้อมูลควรเป็นบัญชีอ่านข้อมูล และแนะนำ authentication แบบ `mysql_native_password` สำหรับความเข้ากันได้ข้ามรุ่น
- ไฟล์ตั้งค่า รหัสผ่าน API key และข้อมูลที่เคยอัปโหลดจะไม่ถูกบรรจุใน GitHub Release

## ไฟล์สำหรับ GitHub Release

- `DataExchangeTools.exe` — ชื่อคงที่สำหรับระบบ Auto Update
- `DataExchangeTools-v0.1.2.exe` — ชื่อระบุรุ่นสำหรับดาวน์โหลดด้วยตนเอง
- `latest.json` — manifest ระบุเวอร์ชัน URL และ SHA256

ผู้ดูแลระบบสามารถเปิดหน้า **ตั้งค่า** แล้วกด **เช็ก Update** เพื่ออัปเดตเป็น `v0.1.2` หลังเผยแพร่ Release และแนบ asset ครบทั้งสามไฟล์แล้ว
