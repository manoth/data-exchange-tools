# Data Exchange Tools v0.1.3

เวอร์ชันนี้แก้ปัญหารายงานตรวจสอบคุณภาพข้อมูลไม่แสดงผล เพิ่มรายงานข้อมูลบุคคล 3 รายการ และทำให้การแสดงเพศในหน้าจอและไฟล์รายงานอ่านง่ายขึ้น

## สิ่งที่เพิ่มและปรับปรุง

### รายงานตรวจสอบคุณภาพข้อมูล

- เพิ่มรายงาน `ตรวจสอบข้อมูลพื้นฐานไม่ครบหรือไม่สมเหตุสมผล` สำหรับบุคคลที่ `PERSON.DEATH = 'N'`
- เกณฑ์อายุผิดปกติคือวันเกิดไม่ถูกต้อง อยู่ในอนาคต หรืออายุมากกว่า 100 ปี
- เพิ่มรายงาน `ตรวจสอบข้อมูล PERSON/PATIENT ขัดแย้งกัน`
- เพิ่มรายงาน `ตรวจสอบคนยังมีชีวิตแต่พบข้อมูลเสียชีวิต` โดยตรวจร่วมกับ `PERSON`, `PATIENT` และ `DEATH`
- แก้ตัวตรวจ SQL ที่เคยมองเครื่องหมาย `;`, `--` หรือคำต้องห้ามภายในข้อความที่ quote เป็นโครงสร้าง SQL
- ยังคงปฏิเสธ SQL หลายคำสั่ง, comment และคำสั่งเขียนข้อมูล เช่น `INSERT`, `UPDATE`, `DELETE`, `DROP` และ `ALTER`
- รีเซ็ตการ์ดสรุป ตาราง และ pagination เมื่อเปลี่ยนรายงาน ป้องกันตัวเลขจากรายงานก่อนหน้าค้างบนหน้าจอ
- ย้ายกรอบข้อมูล/ตัวกรองรายงานไว้ต่อจากหลักการตรวจสอบ เพื่อให้ลำดับการใช้งานชัดเจนขึ้น
- เมื่อเลือกการ์ดผิดปกติ ระบบสร้างปุ่มกรองประเภทความผิดปกติและจำนวนตามนิยามของแต่ละรายงานจาก Control
- ปุ่มกรองรองรับรายงานใหม่ในอนาคตผ่าน metadata โดยไม่ผูกกับหมวดน้ำหนัก/ส่วนสูง

### การแสดงเพศ

- แสดงรหัส `1` เป็น `ชาย` และ `2` เป็น `หญิง` ในตารางของ Agent
- ไฟล์ Excel รายงานคุณภาพข้อมูลและรายงานตรวจสอบสถานะการเสียชีวิตแสดงเป็นชาย/หญิง
- ไฟล์ข้อมูลแลกเปลี่ยนมาตรฐานยังคงเก็บรหัส `1/2` เพื่อรักษารูปแบบข้อมูลเดิม

### ความปลอดภัยและความเข้ากันได้

- รายงานคุณภาพข้อมูลทำงานด้วย `SELECT` และ transaction แบบอ่านอย่างเดียวเมื่อฐานข้อมูลรองรับ
- ไม่เขียนหรือแก้ไขข้อมูลกลับเข้าสู่ HIS
- รองรับ MariaDB 5.x และรุ่นที่สูงกว่าตามกลไก compatibility ที่เพิ่มไว้ใน v0.1.2

## ข้อควรทราบก่อนอัปเดต

- ต้องอัปเดต API Center ให้มีนิยามรายงานล่าสุดก่อนปล่อย Agent v0.1.3
- หลังอัปเดต `server/api/src/services/data-quality-report.service.ts` ต้องรัน build, migration และ restart API Server
- ไฟล์ตั้งค่า รหัสผ่าน Agent API key และไฟล์ข้อมูลผู้ป่วยไม่ถูกบรรจุใน GitHub Release

## ไฟล์สำหรับ GitHub Release

- `DataExchangeTools.exe` — ชื่อคงที่สำหรับ Auto Update
- `DataExchangeTools-v0.1.3.exe` — ชื่อระบุรุ่นสำหรับดาวน์โหลดด้วยตนเอง
- `latest.json` — manifest ระบุเวอร์ชัน URL และ SHA256
