# Data Exchange Tools v0.1.7

เวอร์ชันนี้แก้ปัญหา Windows EXE เปิดได้เฉพาะหน้า `กรุณาสร้างไฟล์ static/index.html` โดยคงความสามารถทั้งหมดจาก v0.1.6

## สิ่งที่แก้ไข

- แก้รูปแบบ `--add-data` ให้ตรงกับ PyInstaller 6 เพื่อฝังโฟลเดอร์ `static` เข้าใน EXE บน Windows ได้ครบถ้วน
- กำหนด PyInstaller เป็นรุ่น `6.21.0` เพื่อให้ผลการ build สอดคล้องกันทุกเครื่อง
- ตรวจก่อน build ว่า source มี `static/index.html` จริง
- เพิ่มการเปิดตรวจภายใน EXE หลัง build ว่ามี `static/index.html` ก่อนสร้างไฟล์ Release
- ถ้าไฟล์หน้าเว็บไม่ถูกฝัง สคริปต์ Release จะหยุดทันที ไม่สร้าง EXE ที่เสียไปเผยแพร่

## ความสามารถที่รวมมาจาก v0.1.6

- การเทียบข้อมูลการตายส่วนกลางแบบแบ่งชุด retry และรองรับเครือข่ายที่ช้าหรือไม่เสถียร
- รายงาน `ตายแล้วมารับบริการ` แบบกลุ่มรายบุคคล พร้อมรายละเอียดวันรับบริการหลังวันที่ตาย
- แสดงวันที่ตายและรหัสสาเหตุการตายจาก API Center
- เมนูจัดการรหัสผ่านผู้ดูแลระบบ

## ไฟล์สำหรับ GitHub Release

- `DataExchangeTools.exe`
- `DataExchangeTools-v0.1.7.exe`
- `latest.json`
