# Data Exchange Tools v0.1.4

เวอร์ชันนี้เพิ่มระบบจัดการการเผยแพร่รายงานคุณภาพข้อมูลจาก Control พร้อมคลังรายงานทางเลือกใน Agent และปรับหน้าตรวจสอบเวอร์ชันให้อัตโนมัติและเข้าใจง่ายขึ้น

## สิ่งที่เพิ่มและปรับปรุง

### การเผยแพร่รายงานคุณภาพข้อมูล

- รองรับรายงานแบบบังคับให้แสดงกับ Agent ทุกหน่วยบริการ
- รองรับรายงานทางเลือก ผู้ใช้เลือกเปิดหรือปิดได้จากปุ่ม `คลังรายงาน`
- บันทึกการเลือกรายงานทางเลือกแยกตามบัญชีผู้ใช้ในเครื่อง Agent
- รองรับรายงานที่ Control กำหนดให้แสดงเฉพาะบางหน่วยบริการ โดยเทียบรหัสกับ `facility_code` ของ Agent
- รายงานที่ Control ปิดใช้งานจะไม่แสดงและไม่สามารถเรียกใช้งานจาก Agent
- รายงานบังคับและรายงานเฉพาะหน่วยบริการจะแสดงอัตโนมัติและผู้ใช้ซ่อนไม่ได้

### หน้าตรวจสอบเวอร์ชัน

- ตัดปุ่ม `เช็ค Update` ที่ไม่จำเป็นออกจากหน้า Settings
- เมื่อไม่มีเวอร์ชันใหม่ จะแสดงสถานะสีเขียวว่าเป็นเวอร์ชันล่าสุดแล้ว
- ระบบตรวจสอบอัปเดตอัตโนมัติเมื่อเริ่มโปรแกรมและทุก 10 นาที
- ปุ่มอัปเดตจะแสดงเฉพาะเมื่อพบเวอร์ชันใหม่กว่าจริง และระบุเวอร์ชันปลายทางบนปุ่ม
- การเปิดหน้า Settings ซ้ำใช้ผลตรวจใน cache และไม่บังคับเรียก GitHub ซ้ำทันที

### ความปลอดภัยและข้อมูล

- รายงานคุณภาพข้อมูลยังคงอนุญาตเฉพาะ SQL แบบ `SELECT` และไม่เขียนข้อมูลกลับ HIS
- การเลือกรายงานทางเลือกไม่มีผลต่อผู้ใช้หรือหน่วยบริการอื่น
- ไฟล์ตั้งค่า รหัสผ่าน Agent API key และข้อมูลผู้ป่วยไม่ถูกบรรจุใน GitHub Release

## ข้อควรทราบก่อนอัปเดต

- ต้องอัปเดต API Center และรัน migration สำหรับสถานะเผยแพร่รายงานก่อนปล่อย Agent v0.1.4
- ต้อง build และ deploy Control Angular รุ่นใหม่เพื่อให้ผู้ดูแลกำหนดสถานะเผยแพร่และเลือกหน่วยบริการได้
- Agent ต้องมี `facility_code` จากการลงทะเบียน/Heartbeat จึงจะได้รับรายงานแบบเฉพาะหน่วยบริการ
- ถ้ายังไม่ได้อัปเดต API Center รายงานเดิมจะยังแสดงได้ แต่สถานะเผยแพร่ใหม่จะยังไม่ทำงาน

## วิธีอัปเดตสำหรับผู้ใช้ Agent

- ผู้ใช้ v0.1.3 สามารถรอระบบตรวจอัปเดตอัตโนมัติทุก 10 นาที หรือปิดและเปิด Agent ใหม่
- เมื่อพบ v0.1.4 ให้กดปุ่มอัปเดตที่หน้า Settings
- หลังอัปเดต ตรวจ Footer และหน้า Settings ว่าแสดง `v0.1.4`

## ไฟล์สำหรับ GitHub Release

- `DataExchangeTools.exe` — ชื่อคงที่สำหรับ Auto Update
- `DataExchangeTools-v0.1.4.exe` — ชื่อระบุรุ่นสำหรับดาวน์โหลดด้วยตนเอง
- `latest.json` — manifest ระบุเวอร์ชัน URL และ SHA256
