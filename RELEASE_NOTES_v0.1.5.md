# Data Exchange Tools v0.1.5

เวอร์ชันนี้เพิ่มการตรวจหาผู้เสียชีวิตที่ยังมารับบริการ ปรับรายละเอียดข้อมูลการตายจากส่วนกลาง และเพิ่มการเปลี่ยนรหัสผ่านผู้ดูแลระบบอย่างปลอดภัย

## สิ่งที่เพิ่มและปรับปรุง

### รายงานตายแล้วมารับบริการ

- เพิ่มเมนู `ตายแล้วมารับบริการ` ในหน้าตรวจสอบคุณภาพข้อมูล
- เทียบเลขประจำตัวประชาชนจาก PERSON กับฐานการตายส่วนกลาง
- ใช้วันที่ตายจาก API Center เป็นเกณฑ์ตรวจ OVST ซึ่งเป็นแหล่งข้อมูลแฟ้ม SERVICE
- เลือกเฉพาะบริการที่มีวันที่มากกว่าวันที่ตาย และสรุปหนึ่งแถวต่อบุคคล
- แสดงจำนวนผู้เกี่ยวข้อง จำนวนครั้งรับบริการ วันที่ครั้งแรก และวันที่ครั้งล่าสุด
- เปิดรายละเอียดเพื่อดูวันที่ เวลา HN, VN และจำนวนวันหลังเสียชีวิตของทุกบริการได้
- การอ่าน HIS ใช้ transaction แบบอ่านอย่างเดียวและไม่แก้ไขข้อมูลใน HIS

### รายละเอียดข้อมูลการตายส่วนกลาง

- หน้าตรวจสอบสถานะการเสียชีวิตแสดงวันที่ตายและรหัสสาเหตุการตาย ICD-10
- รายงานตายแล้วมารับบริการแสดงวันที่ตายและสาเหตุทั้งในตารางและหน้ารายละเอียด
- ผลอัปโหลด/แปลงข้อมูลที่มีการเทียบตายแสดงวันที่ตายและสาเหตุจากส่วนกลาง
- ไฟล์ Excel และประวัติผลแปลงรองรับข้อมูลวันที่ตายและสาเหตุเช่นเดียวกับหน้าจอ
- API Center ยังคงตอบ `matchedPids` สำหรับ Agent รุ่นเดิม และเพิ่ม `matchedPersons` ที่มี `deathDate` กับ `deathCauseCode`

### จัดการรหัสผ่านผู้ดูแลระบบ

- หน้า Settings แสดงเป็นปุ่ม `จัดการรหัสผ่านผู้ดูแลระบบ`
- เปิดแบบ Modal และต้องยืนยันรหัสผ่านเดิมก่อนตั้งรหัสผ่านใหม่
- เมื่อเปลี่ยนสำเร็จ ระบบออกจากระบบและให้ Login ใหม่
- เมื่อไม่สำเร็จจะแจ้งเตือนโดยยังสามารถปิด Modal ได้
- ทุกครั้งที่เปิด Modal ใหม่ แบบฟอร์มและสถานะเดิมจะถูกล้าง

## ความปลอดภัยและข้อมูล

- Agent ส่งเฉพาะ CID ที่ต้องการเทียบไปยัง API Center
- API Center ตอบเฉพาะรายการที่ร้องขอ พร้อมวันที่ตายและรหัสสาเหตุ
- ไม่มีการเขียนสถานะหรือข้อมูลการตายกลับฐาน HIS
- ไฟล์ตั้งค่า รหัสผ่าน Agent API key ข้อมูลผู้ป่วย และไฟล์ผลลัพธ์ไม่ถูกรวมใน GitHub Release

## ข้อควรทราบก่อนอัปเดต

- ต้อง deploy API Center รุ่นที่ตอบ `matchedPersons[].deathDate` และ `matchedPersons[].deathCauseCode` ก่อนใช้งานรายละเอียดครบถ้วน
- ถ้า API Center ยังไม่ส่ง `deathCauseCode` ช่องสาเหตุจะแสดง `-` โดยรายงานส่วนอื่นยังทำงานได้
- การ Build EXE ต้องทำบน Windows จาก source commit เดียวกับ tag `v0.1.5`

## วิธีอัปเดตสำหรับผู้ใช้ Agent

- ผู้ใช้ v0.1.4 สามารถรอระบบตรวจอัปเดตอัตโนมัติ หรือปิดและเปิด Agent ใหม่
- เมื่อพบ v0.1.5 ให้กดอัปเดตจากหน้า Settings
- หลังอัปเดต ตรวจ Footer และหน้า Settings ว่าแสดง `v0.1.5`

## ไฟล์สำหรับ GitHub Release

- `DataExchangeTools.exe` — ชื่อคงที่สำหรับ Auto Update
- `DataExchangeTools-v0.1.5.exe` — ชื่อระบุรุ่นสำหรับดาวน์โหลดด้วยตนเอง
- `latest.json` — manifest ระบุ version, URL และ SHA256
